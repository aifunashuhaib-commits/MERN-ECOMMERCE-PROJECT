# MERN E-commerce (Basic) - Frontend

## Setup
1. Install dependencies:
   ```
   cd frontend
   npm install
   ```
2. Start dev server:
   ```
   npm run dev
   ```
3. By default frontend calls backend at http://localhost:5000 . If your backend is elsewhere, set VITE_API_URL in a .env file.
